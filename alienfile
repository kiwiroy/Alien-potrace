# -*- mode: perl -*-
use alienfile;

plugin 'Probe::CommandLine' =>
  (command => '...', args => ['--version']);

share {
  start_url '';
  plugin Download => (filter => qr//, version => qr//, bootstrap_ssl => 1);
  plugin Extract => 'tar.gz';
  plugin 'Build::Autoconf';
}
